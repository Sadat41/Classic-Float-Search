{
  "name": "classic-float-search",
  "version": "1.1.0",
  "description": "Browser extension to restore classic CSFloat search",
  "scripts": {
    "build": "node build.js",
    "package": "npm run build"
  },
  "devDependencies": {
    "archiver": "^5.3.1"
  }
}